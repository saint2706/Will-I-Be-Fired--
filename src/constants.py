"""Global constants for the termination prediction project.

This module centralizes configuration constants used across the project,
including the global random seed for reproducibility.
"""

# Global random seed for reproducibility across all libraries
# (NumPy, scikit-learn, Python's random module)
RANDOM_SEED = 42
